Function: main
		Parameter: x 
		Body:
		  s = int_of_string(read());
		  print(checkyear(s));
		EndBody.

		Function: checkyear
		Parameter: s 
		Body:
		  If s % 4 != 0 Then Return "Normal";
		  ElseIf s % 400 == 0 Then Return "Leap";
		  ElseIf s % 100 == 0 Then Return "Normal";
		  Else Return "Leap";
		  EndIf.
		EndBody.