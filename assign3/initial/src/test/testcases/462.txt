Function: condition
		Parameter: in, expect
		Body:
		  If (in =/= float_to_int(1)) Then Return 0;
		  ElseIf (in <=. 2.0) Then Return 1;
		  Else 
		    Var: z;
		    print(z);
		    Return -1;
		  EndIf.
		EndBody.

		Function: main 
		Body:
		  Var: i, z = False;
		  For (i = 0, 10, 1) Do 
		    print(string_of_int(condition(float_to_int(i) +. 1.0, z)));
		  EndFor.
		EndBody.